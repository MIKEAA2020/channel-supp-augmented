# GLM contributions (waves 1-33)

This folder collects this assistant's deliverables for the
`channel-supp-augmented` project. The original `manuscript uploads/` and
`wave 2 audits/` folders are untouched; wave 4 produced corrected new
versions of the manuscripts in `manuscript uploads v3/`; wave 5 applied the
flat-width attack as new versions (v4) in `manuscript uploads v4/`; wave 6
completed the attack as new versions (v5) in `manuscript uploads v5/`; wave 7
closed the bottom index as new versions (v6) in `manuscript uploads v6/`; wave 25
integrated the machine-certified core --- the audited `H_2(B_3) = Z/3` ⟹ `delta_2 = 4/3`
chain of Waves 13C/24 --- as new versions (v7) in `manuscript uploads v7/`; wave 27
integrated the W26 near-collision citations and positioning (the Guerra-Jana line,
the Auerbach school, the Z_p-BU background) as new versions (v8) in
`manuscript uploads v8/`; wave 28 delivered the dedicated re-implementation
session (the independent second implementation of the 13C-7 chain, closing
bridge-audit residual (i)) and the v9 manuscript alignment (title, abstract,
keywords, and every affected section updated to the surviving findings); wave 29
delivered the machine-free derivation (the Guerra-Jana transgression route
lifted to integral coefficients): the hand proof of x^2 != 0 and the full
cohomology of B as Proposition prop:cupsquare, integrated as new versions
(v10) in `manuscript uploads v10/`; wave 30 delivered the referee-style read
of the new Proposition `prop:cupsquare` (an independent line-level
re-derivation and an exact-integer corroboration script: the main assertions
confirmed, one substantive correction found in the H^6-generator clause,
six one-line repairs); wave 31 applied the R1 correction as new versions
(v11) in `manuscript uploads v11/` (the instruments paper only: the orbit-sum
lattice statement, the corrected H^6 displays, and the strengthened
lattice-index gate G3L in the verifier; the main article needs no change and
remains at v10); wave 32 adjudicated and applied the six one-line R2-R7
repairs as new versions (v12) in `manuscript uploads v12/` (the instruments
paper only: each repair verified to address its root cause before
application --- including the load-bearing b-pin of Step 4 and the corrected
two-step d_r-vanishing justification, in which the referee's own sketch was
itself imprecise and was corrected before use; the main article stays at
v10); wave 33 delivered the second referee pass over the v12 proof (the
seam read: all eleven repair hunks confirmed present and correct in
context, all superseded phrasings gone, the cross-step dependencies
resolved, 178 independent exact-integer checks all PASS --- verdict:
accept, the referee cycle closed, two optional one-clause polish items
recorded; no manuscript change).

| Path | What it is |
|---|---|
| `WAVE33_SECOND_REFEREE_PASS.md` | **Wave 33.** THE SECOND REFEREE PASS OVER THE V12 PROOF (the user directive: "a second referee-style pass over the v12 proof to confirm the repairs read cleanly end-to-end"; the instruments paper v12, source lines 1857-2138 + the integration points). **Method:** the full seven-step proof re-read and re-derived by hand a SECOND time with the repairs in place --- this time as an explicit SEAM reading (each repaired passage checked in context: dangling dependencies, contradictions with unrepaired text, claims silently changed by the patches) --- plus a fresh exact-integer verifier written from scratch for this pass (`w33_second_pass_check.py`, **178 checks, all PASS, exit 0**, independent of the manuscript, the machine data, and the w30/w31/w32 scripts; 13 gate families: P1-P5 the textual seam layer --- 38 repaired-phrase presences, 7 superseded-phrase absences, .txt == .tex, the main-article sync + no proof internals, the notation-order scan --- and M1-M8 the mathematical layer --- the lattice indices 1,1,1,2,2,4,8 with the witness, the tau-rule norm model + ker N = (T-1)M for d <= 8, the crux SNF + the R4 counterfactual + the R5 sum-witness, the degree-6 quotient in the paper's coordinates with the membership triple, the NEW lift-independence gate ([Delta + c tau^3] = 2[S1] for c = 0,1,2 --- the tau^3-tail dies in the quotient, which is what makes the statement's "discriminant class" well defined in H^6(B)), the sigma_1-divisibility lattice fact re-verified for degrees 1-8 plus the sigma_2-side bookkeeping for degrees 2-8, the pins in Z[u]/(3u), the degree runs, R6/R7). Honest debug trail: four bugs in the checker's own code caught before any conclusion (a shadowed function name, an orbit-basis ordering assumption, a wrong kernel-basis convention in the SNF routine --- the kernel of P.A.Q = S is the columns of Q, not Q^-1 --- and a degree mismatch in the sigma_2 evaluation), each fixed in place, battery re-run. **VERDICT: the repairs read cleanly end-to-end --- accept, no mandatory corrections.** All eleven hunks (R1's five + R2-R7's six) present, seam-clean, and independently re-verified; the forward-dependency structure sound (Step 1's odd-r clause marked "by Step 3 below" and SUPPORTED by Step 3's closing sentence); the load-bearing R4 pin supplies Step 5's completeness rather than leaving it silent; the statement <-> Step 7 consistency including the lift-independence of the H^6(B)-discriminant. **Two residual nano-nits recorded as optional one-clause polish (no mathematics affected):** F1 --- Step 1's repaired text uses sigma/tau (and the Step-4 value sigma_2+2tau^2) 38 lines before their definitions, the even-r clause's forward dependencies unmarked (the odd-r clause carries its marker), plus the lead-in word "boundaries" conflating boundaries with non-cycles (the elaborating sentences are precise); F2 --- Step 7's orbit-sum classes are defined only up to tau^3-tails (Step 3's display chooses a splitting; only A_1 and the pinned sigma_i are canonical), and the quotient is lift-independent because 2 tau^3 = 0 kills the ambiguity --- verified, but the paper never says so; half a clause would close it. Integration points all clean (Lemma 6.17 unchanged and accurate; rem 6.19's residuals list now matching what G3L certifies; rem 6.18 accurate; the main article correctly stays at v10 --- prose-level sync, no proof internals, verified). **No manuscript change in this wave** (the two polish items are recorded for a future revision's convenience, not applied --- they are sub-threshold, the user's conditional-repair discipline being the standing rule). The epistemic chain on the Proposition: hand-derived (W29), machine-certified (13C), independently re-implemented (W28), referee-verified (W30), lattice-gated (W31), referee-complete (W32), **second-pass confirmed (W33)**. Qutrit verdict unchanged: H_2(B_3) = Z/3, delta_2(D(C^3)) = 4/3; ququart OPEN [4/3, 3/2]; the W23 stop stands. Artifacts: the verifier + transcript + json, this note, w33_report.tex + the compiled PDF (tectonic exit 0, 3 pages, fonts embedded, metadata set, zero overfull boxes; the two residual QA flags disproven/expected: the widest text edge measures exactly at the geometry margin, and the title-block convention is the W30 format). |
| `WAVE32_CORRECTION_V2.md` | **Wave 32.** THE V12 CORRECTION WAVE (the user directive: "apply the six one-line R2-R7 repairs only if truly merited and addressing root cause of issues"). **Adjudication before application** (w32_r2r7_check.py, 12 gates, all PASS, exit 0, written independently of the manuscript and the machine data): each R2-R7 site confirmed verbatim in the v11 source, each proposed repair re-derived by hand, each repair's own arithmetic machine-verified. **Verdict: all six merited.** R2 (the d_r-vanishing parenthetical): the v11 justification is FALSE as written (H^odd(U(3)) = 0 --- but the fibre cohomology has odd generators; gate R2A); the referee's own proposed rewording was itself imprecise (the sigma_1-multiple coefficients in fibre-degree eight are d2-BOUNDARIES via z1z3z5, not d4-victims --- for beta = sigma_1, d4(sigma_1 z3 z5) = sigma_1 sigma_2 z5 is zero on the E_4-page), so the APPLIED repair is the corrected two-step: odd r dead by H^odd(BK) = 0 (gate R2B: ker N = (T-1)M lattice-exactly, d <= 8); even r >= 8 confined to fibre degrees 8-9 (gate R2C) with the killing computations (gates R2D) and a NEW fact the sketch lacked --- the sigma_1-divisibility lattice fact (beta.sigma_2 a sigma_1-multiple forces beta a sigma_1-multiple: gate R2E verifies {beta in M_d : sigma_1 | beta} = sigma_1.M_{d-1} as lattices, degrees 1-8). R3: the (2,2)-slot vanishing clause (H^2(U(3)) = 0, the exterior algebra has no degree-2 class). R4 (LOAD-BEARING, the strongest): the v11 uniqueness parenthetical is invalid (the restriction sees only the free part; the coset sigma_1 + b tau); unpinned, d2(tau z1) = b tau^2 would inject the extra relation into E_inf^{4,0} and THE CRUX WOULD DIE (gate R4B computes the counterfactual exactly: the quotient becomes 0); applied: iota*c_1 = sigma_1 + b tau in the display + the pin 0 = s*(iota*c_1) = bu in the splitting chain (gate R4A). R5: the separate-subgroup phrasing replaced by the explicit two-step (split off the direct summand Z sigma_1^2, then the single infinite-order relation); the SUM-quotient verified Z/3 with [tau^2] generating (gate R5, with the (2,2)-in-Z^2 witness that separate non-membership does not imply sum non-membership). R6: the deck-convention remark (the identification passes through the inversion K\\U(3) = U(3)/K; at worst a sign, which squares away in x^2 = gamma*(tau^2); gate R6). R7: tau.Delta marked lift-dependent (the class of Delta is fixed only up to a tau^3-tail; tau.(Delta + c tau^3) = c tau^4), with the sigma_i-instances noted unconditional (pinned pure by Step 4); unused downstream (gates R7A/R7B). **The edit** (edit_v12.py, 6 anchored hunks, each matched exactly once, net +29 lines, .txt == .tex): v12 = the instruments paper only, the main article stays at v10 (all six sites are internal to the proof of Proposition 6.16). **QA:** tectonic exit 0, 51 pages, zero errors/undefined refs; the number-free warning multiset byte-identical to the v11 baseline with every region shifted by exactly -29; check_v12.py 96 PASS / 0 failures (all repairs present, all superseded phrasings gone, the v11/R1 + v10 must-holds intact, the W26 artifacts annotated, +74 dollars = 37 new math pairs); PDF render verified phrase-by-phrase; the full Wave-31 derivation battery re-run: all 12 gates PASS (the math layer unchanged). The referee's "accept with minor revision" verdict is now fully implemented on BOTH axes (R1 by Wave 31, R2-R7 by this wave). Qutrit verdict unchanged: H_2(B_3) = Z/3, delta_2(D(C^3)) = 4/3; ququart OPEN [4/3, 3/2]; the W23 stop stands. |
| `WAVE28_REIMPLEMENTATION.md` | **Wave 28.** The dedicated re-implementation session + the v9 alignment. **Part 1, the independent re-implementation of the 13C-7 chain** (the W27 §4 bounded merited item; closes bridge-audit residual (i)): three fresh modules (w28_base.py, w28_pins.py, w28_total.py) re-deriving the whole chain with fresh algorithms at every layer --- the base by exact region membership + outward-normal determinants (G0 by exact sympy SNF; **all 6 data fields MATCH** the committed 13C-1 data), the pins by the column-ratio phase method (vs the BFS gauge solve; Newton inversions; SB by constraint propagation from dT=Td vs Jacobians; **all 7 pin families MATCH, spread 0.000**), and the total complex with fresh T2 labels, the q-map derived from the linear projections, the homotopy re-derived from its identity, the seam system solved by direct Z/12 unit-pivot elimination (the solution vector equals the original's), and the homology by fresh elimination with different primes (1000037/1000081, torsion {3,17,19,23,29,31}) + an incremental subgroup-order Z/9 algorithm: **all eleven battery gates PASS on the freshly assembled 14,910-cell complex and the tuple H_*(B_3) = (Z, Z/3, Z/3, Z/3, Z/3, 0, Z) REPRODUCES** (Z/9 ladder (9,3,9,9,9,3,9), PD cross-check MATCH); falsification tests show the convention data is forced (each flip breaks dT=Td or the orbit homology); the honest negative finding: the WDIR/SEAM_DIR parameterization is convention-robust (not forced). Residuals: spec-level risk remains (two implementations, one spec); the geometric warrant unchanged; the machine-free derivation NOT attempted (the next merited item). **Part 2, the v9 manuscripts** (edit_v9.py, 10+3 anchored hunks): the instruments title gains 'Flag-Quotient Width Obstructions'; the abstract gains the flat-width classification + the doubly-implemented premise status (the v8 abstract predated the W25 block); keywords gain the cohomological vocabulary; rem:machine-certificate's residuals updated (the fresh algorithms named, p<=31 scan, the falsification); open problem (vi)'s re-implementation clause discharged; 5 further provenance updates; the main article's abstract/keywords/sync-clause aligned. tectonic clean (48/46 pages); check_v9: 63 PASS, the 3 raw flags are the W26-diagnosed artifacts (200/197, 237/233, the TikZ $ count). |
| `WAVE29_MACHINE_FREE_DERIVATION.md` | **Wave 29.** THE MACHINE-FREE DERIVATION (the W27 §4 high-merit objective, the Guerra-Jana route, integral lift). **The derivation** (w29_derivation_check.py, 11 gates, all PASS): B = U(3)/K with K = T³⋊⟨σ⟩ the cyclic monomial subgroup; the Borel fibration U(3) → B → BK exhibited as the pullback of the universal fibration along Bι ⟹ the Chern transgressions d₂(z₁) = σ₁, d₄(z₃) = σ₂ + 2τ², d₆(z₅) = σ₃ (ε = 2 pinned by the BC₃ splitting + the regular-representation Chern classes); H*(BK;Z) by hand via the collapsed BT³-SS (parity + permutation-module argument): the free part Z[σ₁,σ₂,σ₃] ⊕ Δ·Z[σ₁,σ₂,σ₃] + the torsion with the τ-detection rule τ·f = [the (χ₁χ₂χ₃)^{d/3}-coefficient of f mod 3]; THE CRUX: the transgression images ⟨σ₁²⟩ and ⟨σ₂+2τ²⟩ are generated by infinite-order classes, so the order-3 class τ² survives in H⁴ = (Zσ₂ ⊕ Z/3τ²)/⟨σ₂+2τ²⟩ = Z/3, and the edge gives x² = γ*(τ²) ≠ 0; by the W24 P5 pattern the d₃^{1,2} bit is decided by hand ⟹ lem:flag-cohomology complete (H³ = H⁴ = Z/3, x² generates). The full tuple H*(B) = (Z,0,Z/3,Z/3,Z/3,Z/3,Z) derived (H⁶ = Z·Δ, the discriminant). The derivation uses NO machine data (G10 reads the wave13c/28 tuple only as corroboration). Honest debug trail: three gates caught three implementation bugs (the module-rank formula, the image-vector combination, the Leibniz signs via the D²=0 gate). **The v10 manuscripts** (edit_v10.py, 10+2 anchored hunks): the new Proposition prop:cupsquare with the seven-step proof; the lemma's machine-decision paragraph replaced by the hand decision; rem:machine-certificate rewritten (hand-derived + the machine as cross-check + the three-independent-routes closing + the lens-model validation of the transgression route); rem:flag-literature's integral-lift note; open problem (vi) DISCHARGED; the abstract/intro/conclusion provenance phrases; the HatcherSS bibitem; the main-article sync clauses. tectonic clean (51/46 pages, zero errors; warnings all pre-existing, verified against the hunk boundaries); check_v10: 53 PASS, the 3 raw flags byte-identical to the v9-diagnosed artifacts. **thm:state-d2 is now fully hand-checkable** — the flagship δ₂(D(C³)) = 4/3 rests on classical-cited + line-checkable mathematics only, with two machine routes as independent cross-checks. |
| `WAVE30_REFEREE_READ.md` | **Wave 30.** THE REFEREE READ OF PROPOSITION `prop:cupsquare` (the user directive; v10 instruments, source lines 1857-2084 + the integration points). Independent method: every step re-derived by hand + every checkable arithmetic claim re-computed from scratch by `w30_referee_check.py` (7 check families; machine data read only as final comparison). **CONFIRMED:** Step 0 (the homogeneous model), Step 1 (the pullback of the universal + trivial local system), Step 2's groups (the collapse argument airtight; free ranks = Burnside orbit counts; torsion in bidegrees (2k,6a)), Step 3's τ-rule (all 10 products re-derived from the norm model: τσ₁=τσ₂=τΔ=τσ₁³=τσ₁σ₂=0, τσ₃≠0) and the H⁴(BK)=ℤσ₁²⊕ℤσ₂⊕ℤ/3τ² lattice (index 1 — the degrees the crux needs are safe), Step 4's pins (c(reg): c₁=3u=0, c₂=2u², c₃=0 ⟹ ε=2, ε'=0; and c₁'s constant b=0 via the same splitting), Step 5 THE CRUX (SNF of [[1,2],[0,3]]: E∞^{4,0}≅ℤ/3, [τ²] of order 3 generating, [σ₂]=−2[τ²]), Step 6 (κ=π∘γ rigorous by associated-bundle functoriality; x²=γ*(τ²)≠0, robust to the ±-sign convention), Step 7's degree runs (incl. the injectivity of d₄ on z₃ — the infinite-order image, correctly contrasted with the lens model where 3z₃ survives), the tuple (Z,0,Z/3,Z/3,Z/3,Z/3,Z) + UCT vs machine, and the Lemma 6.17 two-worlds logic. **FINDING R1 (substantive, mandatory correction):** Step 2's integral module statement Z[σ]⊕Δ·Z[σ] is FALSE as a lattice statement — the true free part is the orbit-sum lattice, the module is index 2 in fibre-degree 3 (witness S₁=½(σ₁σ₂−3σ₃+Δ), the system forces b=e=½) with the index growing (2,2,4,8,16,32 in degrees 3-8); propagating to Step 3's H⁶(BK)-display (non-generating), Step 7's "≅ℤ⟨Δ⟩", and the STATEMENT's clause "a discriminant class generating H⁶" — in H⁶(B;Z)≅ℤ the discriminant is TWICE a generator ([Δ]=2[S₁], the orbit-sum class; membership-tested exactly: Δ−2S₁ in the image lattice, Δ−S₁ and S₁ not). The groups everywhere, the crux, the lemma, thm:state-d2, δ₂(D(C³))=4/3 all UNAFFECTED (the repair is ~six lines; the rational ℚ⊕ℚΔ̄ statement survives). Root cause: G3 checks RANKS only — an index-2 sublattice is invisible to it; a lattice-index gate is recommended. **Findings R2-R7** (one-line repairs): Step 1's d_r-vanishing parenthetical garbled (H^{odd}(U(3))≠0; correct: odd r ⟹ H^{odd}(BK)=0, even r≥8 ⟹ sources already dead), the silent (2,2)-slot vanishing, Step 4's c₁-uniqueness parenthetical invalid (pin b=0 by the splitting), the crux's per-subgroup phrasing (closed by the direct-summand two-step quotient), Step 6's convention sign (squares away), τΔ's unpinned torsion tail (unused downstream). Integration assessed: rem 6.19's "self-contained invariant-theory step" + "invariant lattice verified" phrases need adjustment after R1; the Guerra-Jana differentiation and the (vi) discharge stand. **Verdict: accept with minor revision.** |
| `WAVE31_CORRECTION_V1.md` | **Wave 31.** THE V11 CORRECTION WAVE (the user directive: "the ~six-line R1 repair and the strengthened lattice-index gate"; R2-R7 were not in the charge and remain recommendations). **The R1 repair** (edit_v11.py, 5 anchored hunks, each matched exactly once; the v10→v11 diff confined to the five intended regions, net +25 lines): (1) the Proposition statement's clause "a discriminant class generating H⁶" → "H⁶(B;Z) ≅ Z, in which the discriminant class is twice a generator"; (2) Step 2's integral module equality Z[σ]⊕Δ·Z[σ] → the orbit-sum invariant lattice (integral; spanned by the C₃-orbit sums, equal-coefficient justification) with the rational module Q[σ]⊕Δ·Q[σ] stated over ℚ and the finite-index note (index 1 in degrees ≤ 2 — the crux's degrees; 2, 2, 4, 8 in degrees 3-6) + the degree-3 witness S₁ = χ₁²χ₂+χ₂²χ₃+χ₃²χ₁ = ½(σ₁σ₂−3σ₃+Δ) in the text; (3) Step 3's H⁶(BK) display → the orbit-sum basis Z{A₁,S₁,S₂,σ₃} ⊕ ℤ/3·τ³ with the relations S₁+S₂ = σ₁σ₂−3σ₃, S₁−S₂ = Δ and the index-two note (H⁴(BK) untouched — already exact); (4) Step 7's degree-6 run on the orbit-sum lattice: H⁶(B;Z) = (Z{A₁,S₁,S₂,σ₃} ⊕ ℤ/3·τ³)/⟨σ₁³, σ₁σ₂, 2τ³, σ₃⟩ ≅ Z·⟨S₁⟩ with the substituting chain and the conclusion Δ = S₁−S₂ = 2S₁ twice a generator (rational ℚ⊕ℚΔ̄ retained); (5) rem:machine-certificate's residuals list names the lattice layer the gate certifies. **The strengthened gate** (w31_derivation_check.py = the wave-29 verifier + the new G3L; the w29 script/transcript preserved as historical artifacts; w31 re-runs the full battery — **all 12 gates PASS**, exit 0): G3L certifies the lattice-index layer — L_d ⊆ M_d with [M_d:L_d] = 1,1,1,2,2,4,8,16,32 (degrees 0-8, SNF of the change-of-basis), the safe-degrees property (index 1 for d ≤ 2 — H⁴(BK)'s display exact), the four orbit-sum identities, the degree-3 witness (the unique rational solution (0,½,−3/2,½) — not integral; 2S₁ ∈ L₃), and the top-class quotient (relations [[1,3,3,6],[0,1,1,3],[0,0,0,1]] in the invariant basis; SNF invariant factors (1,1,1) → M₃/⟨σ₁³,σ₁σ₂,σ₃⟩ ≅ ℤ; the membership triple: Δ−2S₁ in the relation lattice, Δ−S₁ and S₁ not → H⁶(B) = Z·⟨[S₁]⟩, [Δ] = 2[S₁]; gcd(2,3)=1 for the torsion death). Honest debug trail: G3L's first run caught two bugs in my own gate code (a transposed identity and a mislabelled witness printout — the values were right, the presentation wrong); both fixed, battery re-run. **QA:** tectonic exit 0, 51 pages (v10 was 51), zero errors, zero undefined refs; every box warning byte-identical to the v10 baseline (including the two-pass 3.34/11.59pt pair on the untouched flag-literature paragraph); check_v11.py: 0 failures — all corrected statements present, all superseded v10 statements gone, all v10 must-hold checks intact, and the 3 W26 artifacts now annotated in-checker (211/208, 237/233, TikZ 3073; +52 $ = exactly 26 new math pairs, parity even); PDF render verified phrase-by-phrase (both displays, the identities, the twice-a-generator conclusion, both remark phrases). **Supersession note (R1 part 6):** wave 29's "H⁶ = Z·Δ" record is superseded by H⁶(B;Z) = Z·⟨S₁⟩ with [Δ] = 2[S₁]; the additive tuple (Z,0,ℤ/3,ℤ/3,ℤ/3,ℤ/3,ℤ) is unaffected (it records the group, not the generator); the main article needs no change (referee-confirmed, grep-verified) so v11 = the instruments paper only. |
| `WAVE27_V8_CITATIONS.md` | **Wave 27.** The v8 citation-integration pass (the W26 verdict's only blocking item, now closed): the positioning remark `rem:flag-literature` in the instruments paper (Guerra-Jana TAMS 378 (2025) 3507-3550; Guerra-Jana-Maiti Topology Appl. 365 (2025) 109279; Weber-Wojciechowski CCM 19 (2017) 1750016; Korbas-Lorinc Fundam. Math. 178 (2003) 143-158; Matszangosz Math. Ann. 381 (2021) 1537-1588; the Z_p-BU pair Singh Topology Proc. 36 (2010) / Crabb arXiv:2211.08087) with the precise differentiation (cyclic intermediate quotient vs symmetric-group; integral 3-torsion with exponents pinned vs field coefficients; equal-value/width chain vs Auerbach counting), the intro cross-ref, the open-problem (vi) candidate-route clause, seven bibitems; the main article sync-clause differentiation + two bibitems. Every field live-verified (Crossref/zbMATH Open/arXiv/DOI probes, logs in `novelty_pass/v8_*`). TWO HONEST CORRECTIONS to the W26 record: the 'uncited NechitaEtAl2018' finding is RETRACTED (the citation exists as \cite[Corollary~2]{...}, the v7 checker regex missed the optional-argument form; check_v8.py fixed, zero uncited bibitems now), and the cup-length paper is Korbas AND Lorinc (two authors). tectonic clean (48/46 pages, no errors; all box warnings pre-existing); check_v8 all PASS. Also: the task-2 merit verdict (machine-free derivation = the high-merit research objective, the independent re-implementation = the bounded next-session item; nothing rushed into this session by design) and the task-3 MathSciNet access report (account-level LibLynx wall --- all review pages unreachable, MR reviews carry no DOIs; every v8-relevant paper reachable via its open route; the DOI table in the note). |
| `WAVE25_MANUSCRIPT_INTEGRATION.md` | **Wave 25.** The certified core integrated into the manuscripts as v7: the lemma `lem:flag-cohomology` (H₃(B) = H₄(B) = ℤ/3 with x² generating H⁴; the CLSS total-3/4 page derived by hand, the d₃ bit not page-decidable, the decision machine-certified) with `rem:machine-certificate` (the 14,910-cell battery, the four external anchors, the lens/ℝP²×S² model-case validation, the honest residuals, the repository pointer); the plane-valued equal-value-basis theorem `thm:equal-basis-plane` (the r=2 telescoping H: Fl → ℝ⁴ with T = T₁⊗I₂, the paper's own conjugate-then-normalize step one level up, the section of L⊕L, e = c₂ = x² ≠ 0); the second-index width theorem `thm:state-d2` (δ₂ ≥ 4/3 for every d_B ≥ 3, exactly 4/3 at the qutrit, [4/3, 2(1−1/d_B)] at d_B ≥ 4) with the decoder-scope remark; the consistency edits at seven further places (intro, roadmap, `cor:flat-fails`, `rem:flat-status` (open range r ≥ 3), `con:coord-flat` corrected a second time (clause r ≥ 3), conclusion, open problems ((v) updated, (vi) new)); the main article's open-problem (1) sync clause. Both v7 papers compiled with tectonic (47/46 pages, no errors). |
| `WAVE26_NOVELTY_PASS.md` | **Wave 26.** The full-text novelty pass (merited-queue item 4) + the v7 flaw audit: the search battery (23 web + arXiv website + zbMATH Open, all logs in `novelty_pass/`): **no collision on any theorem form** (the exact widths, the equal-value-basis theorems, the instrument-body exact geometry, the query-uniform framework); the one near-collision line: Guerra-Jana 2023-2025 (unordered flag quotients + Auerbach bases, arXiv:2309.00429 TAMS / arXiv:2304.12990 Topol. Appl.) — same seam, disjoint spaces (Sigma_n vs the cyclic C_3), coefficients, and applications; **the v7 citation gap is the one blocking item for submission**; the flaw audit: mechanical/compile/line-level all PASS (the E2 page of `lem:flag-cohomology` independently re-derived by hand), the two genuine minor findings (the uncited `NechitaEtAl2018` bibitem in MAIN; the missing citations above); nothing fabricated. |
| `WAVE7_FLAT_WIDTH_ATTACK_3.md` | **Wave 7 (this round).** The two residual state-space cases closed in the negative direction via the **equal-value-basis theorem** (`thm:equal-basis`: every continuous `f` on the qutrit's pure states admits an orthonormal basis with all three values equal — flag manifold, `ℤ/3` cycle, telescoping `H`-map, Chern-class obstruction on the flag quotient): `δ₁(D(ℂ³)) = 4/3` **exactly**, `δ₁(D(ℂ⁴)) ≥ 4/3` (bracket `[4/3, 3/2]`, linear encoders provably ≥ `3/2`), so the `con:coord-flat` flat direction **fails at `r = 1` for every `d_B ≥ 3`** and the conjecture is corrected by the clause `(d_B ≤ 2 or r ≥ 2)`. The bottom index `r = 1` is **classified completely** (`cor:bottom-dichotomy`); every surviving `d_B ≥ 3` flatness is gated by the state-space widths through the new concentration gate (`lem:slice-gate`), smallest unknown `δ₂` of the qutrit. Channel open-problem-1 residual closed. Ququart exactness posed with the `d₃` transgression identified as the hinge. |
| `WAVE6_FLAT_WIDTH_ATTACK_2.md` | **Wave 6.** The flat-width attack completed: switching from slot-counting to **coordinate**-counting (`nd_B` flags) yields the **coordinate dichotomy** — `δ_r = 1` iff `nd_B ≤ 2r+2`, proved for `d_B ∈ {1,2}` (complete classical and qubit dichotomies), with `δ_r ≥ 4/3` proved below the diagonal for every `d_B` (elementary median matching at `r = 1`, topological Tverberg at `r ≥ 2`), `δ₁(Δ₄) = 4/3` and `δ_r(2r+3) = 4/3` exactly, `δ_{n−1} = 2(1−1/d_B)` exactly for prime-power `d_B`, the qubit flat threshold improved to `r ≥ n−1`, the v4 refined conjecture refuted at `r ≥ 2`, the channel-level `n = 1` instance refuted for `d_B ≥ 5`, and the corrected statement posed as `con:coord-flat`. |
| `WAVE5_FLAT_WIDTH_ATTACK.md` | **Wave 5.** The flat-width conjecture `con:flat-d1` attacked via the simplex sub-case: **refuted** at the bottom index (`δ₁ ≥ 4/3` for `n ≥ 5`, every `d_B`, full elementary proof), flatness **proved** on the upper half of the subcritical range for classical outputs and from latent dimension `n` for qubit outputs, and the refined statement posed as `con:flat-refined`. Also records the 54-claims ledger reconciliation. |
| `WAVE3_ELEVATION_AND_CORRECTIONS.md` | **Wave 4.** All verified corrections from waves 1-3 applied to the two manuscripts as new versions (v3); every overclaim elevated (math strengthened to meet it) or demoted to an explicit conjecture. |
| `WAVE2_AUDIT_VERIFICATION_REPORT.md` | **Wave 3.** The four `wave 2 audits/` files sorted into 16 attributed audits, every substantive claim verified against the then-current manuscripts, jointly evaluated with the wave-1 report. |
| `sorted-wave2-audits/` | The 16 audits split into one file each, grouped by target paper (see its README). |
| `wave 1 report/` | **Wave 1.** Line-level review of both manuscripts (docx + pdf). Findings are folded into the wave-3 report's consolidated list. |
| `synthesis paper/` | **Wave 0 (earlier rounds).** Corrected synthesis paper (tex + compiled pdf); the tex is identical to `gpt round two/glm_CORRECTED_SYNTHESIS_PAPER2.tex` already in this repo. |

The current manuscripts: the instruments paper lives in
`../manuscript uploads v12/`
(`instruments-paper-revised12.tex/.txt/.pdf`); the main article is unchanged
by waves 31-33 and its current version remains
`../manuscript uploads v10/main-article-revised10.tex/.txt/.pdf`;
the previous corrected versions are in `../manuscript uploads v11/` (the
instruments paper only), `../manuscript uploads v10/` (both
papers), `../manuscript uploads v9/`,
`../manuscript uploads v8/`,
`../manuscript uploads v7/`,
`../manuscript uploads v6/`,
`../manuscript uploads v5/`, `../manuscript uploads v4/` and
`../manuscript uploads v3/`.

Waves 8–24 --- the CLSS verification, the 13B/13C cellulation campaign, the
theory-wave line, the n=4 program (Waves 17–23), the external merit audit, and
the Wave-24 bridge audit --- live in this folder as `WAVE*.md` notes with their
scripts, outputs, and data files; the table above lists the manuscript rounds.
