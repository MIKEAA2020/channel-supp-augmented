# WAVE 19 (c)-SCOPE — THE ADJUDICATION NOTE (Stage 4c: the support-descent below the tau/q cells + E/V connections)

Directive (2026-09-12, English only): "the (c)-scope — the support-descent below the tau/q cells + E/V connections — is now the decisive test for whether one genuine Z/2 remains. push all creations including previous round." Executed as the honest A2/level-2/GKM/S3 reconstruction of the (c)-scope machinery, with the full accumulated queue. The prior session's locked design was recovered from the transcript; its final hand-computation contained one arithmetic slip ((-1,-1,2) mis-copied as -(1,-2,-1), producing a spurious d(F) asymmetry) — corrected here; the code decides.

## (1) THE MODEL (w19_core.py, all gates PASS)

The Z^3 picture: Lambda = {v : sum v_i = 0} with roots alpha1 = e1-e2, alpha2 = e2-e3, rho = e1-e3; the level-2 hexagon H2 = {|v_i| <= 2} = the 19 P-19 corners [machine: 19]; the sectors Lambda/2Lambda with counts (7,4,4) [machine: (7,4,4,4)]; the quadratic refinement Q = sum binom(v_i,2) mod 2, well-defined on Lambda/2Lambda, polarization verified on all 16 pairs; the connection c(v -> v+delta) = (-1)^{<v,delta>} (symmetric gauge) on the 42-edge/24-triangle corner graph [machine: 42/24]; THE CURVATURE GATE: -1 on ALL 24 unit triangles [machine-exact] — this gate pins the connection convention. The character table: psi_a1, psi_a2 (the E-characters) and psi_rho (the V-character), each trivial on its own defining class (<alpha_i, alpha_i> = 2 = 0 mod 2). The E/V root split is STRUCTURAL (alpha1/alpha2 = E, rho = V — the 2 rho-labeled GKM edges), not gauge-dependent: in the oblique gauge the Q-parities read 0,0,1 (the prior session's gauge), in the Z^3 gauge all three are 1 — both are honest refinements of the same alternating form (they differ by x+y).

THE CELL CHARACTERS chi_w = psi_{w rho - rho} (the honest equivariant derivation: the torus acts on cell w with weights Inv(w), so Gamma acts on the cell orientation by the character of sum Inv(w) = rho - w rho): {e, w0}: triv; {s1, s2s1}: psi_a1; {s2, s1s2}: psi_a2. *** THE V-CHARACTER psi_rho NEVER APPEARS AS A CELL CHARACTER *** (the root cause of the vacuity theorem below). The GKM hexagon (the strong Bruhat coverings, = the Inv-inclusion order): 6 edges = 4 E + 2 V (the s1<s1s2 and s2<s2s1 rho-edges). The descent-character identity psi_{u rho - w rho} = chi_u . chi_w holds on all 6 edges (since 2w rho in 2Lambda). The booked Gamma-module structure: H_0, H_6 = triv; H_2 = psi_a1 (+) psi_a2; H_4 = psi_a2 (+) psi_a1 (the swap) — matching the cell census cell by cell. The untwisted flag homology (Z, Z^2, Z^2, Z) verified.

## (2) THE (c)-SCOPE DECISIVE (w19_cscope.py)

(a) THE FIRING CENSUS: per covering (u < w, label beta) and character-book sigma, the entry = 4*[psi_beta = sigma] (the m_beta-projector on the sigma-coinvariant book: sum_gamma psi_beta(gamma) sigma(gamma) = 4 delta). THE E/V STRUCTURE THEOREM [machine-verified]: the V-edges fire ONLY in the psi_rho book; the E-edges only in the E-books; NOTHING fires in the trivial book (no root is 0 mod 2Lambda). d(F) PER BOOK: d(F)^psi_a1 = 4*s2s1; d(F)^psi_a2 = 4*s1s2; d(F)^triv = d(F)^psi_rho = 0 — each E-book fires exactly ONE top facet, the two E-books mirror images. The prior session's "4*s1s2 + 0*s2s1" asymmetry = the transcription slip + a book conflation (it checked the s2s1-facet against the psi_a2-book's character).

(b) THE PER-BOOK TWISTED HOMOLOGIES (the sigma-coinvariant cell complexes): triv: (Z, Z^2, Z^2, Z); each E-book: Z/4 at H_0 and H_4 = Z (+) Z/4; the V-book: (Z/4)^2 at H_2 (the middle — two levels below the tau/q cells). The E/V torsion layer-split is exact: E-firings create torsion at the extremes of the Bruhat filtration, V-firings at its center, and the two never mix.

(c) THE FULL TOTAL COMPLEX T = the cells x K(a) (x) K(b) (the minimal Koszul resolution of Gamma = <a, b>): basis (w, i, j), total degree 2l(w) + i + j; the differential = the Koszul part with the character substitutions (chi_w(g) - 1) / (1 + chi_w(g)) in {0, +-2}; D^2 = 0 EXACT at every degree 2..11 [machine]. The honest homology (integer kernel lattices, saturated, image coordinates solved exactly, SNF of the coordinate matrix — NOT single-boundary cokernels, which an intermediate buggy implementation showed produce spurious free classes): H_0 = Z; H_1 = (Z/2)^2; H_2 = (Z/2)^3; H_3 = (Z/2)^5; H_4 = (Z/2)^8; H_5 = (Z/2)^10; H_6 = Z (+) (Z/2)^13; H_7 = (Z/2)^17; H_8 = (Z/2)^19; *** H_9 = (Z/2)^23 ***; H_10 = (Z/2)^25. The free part vanishes above degree 6 (the finite-group law) and the free classes at 0 and 6 are the flag's own fundamental classes through the Borel construction (the equivariant formality visible in the answer).

*** THE DECISION: H_9(B_4; Z) at this complex = (Z/2)^23, ALL 2-primary summands plain Z/2 (no Z/4 at any degree — 125 summands verified over degrees 1..10). ***

THE PROVENANCE DECOMPOSITION (per cell, the E/V/N label by the cell's character): e [triv/N]: H_9(Gamma; Z) = (Z/2)^6; s1 [psi_a1/E]: H_7 = (Z/2)^4; s2 [psi_a2/E]: H_7 = (Z/2)^4; s1s2 [psi_a2/E]: H_5 = (Z/2)^3; s2s1 [psi_a1/E]: H_5 = (Z/2)^3; w0 [triv/N]: H_3 = (Z/2)^3. TOTAL: E:14 + N:9 + V:0 = 23.

(d) THE V-CORRECTION VACUITY THEOREM [machine]: zero cells carry psi_rho, so removing the V-layer leaves the complex UNCHANGED (generator-for-generator, entry-for-entry): NO V-ARTIFACT EXISTS AT ANY DEGREE. The only place the V-character ever acts is the separate psi_rho-coinvariant book of (b), which does not feed the orbit complex.

(e) THE SUPPORT-DESCENT NO-CUT THEOREM [machine]: T^above = span{i+j+l(w) <= 3} is a subcomplex, and ALL degree-9 generators lie strictly below the tau/q diagonal (p + l = 9 - l >= 6 > 3): H_9(Q) = H_9(T) — the below-diagonal restriction does NOT cut degree 9. The decisive burden falls entirely on the E/V connections.

*** THE VERDICT: RE-OPEN (E-CARRIED). The degree-9 2-primary classes carry ZERO V-provenance; the E/V-artifact mechanism CANNOT kill the degree-9 torsion; the standing FALSE verdict (the W20 conditional) cannot be hardened via (c). Directionally consistent with the W21 re-opening, and stronger: no V-mediated spurious class exists at ANY degree in the analog book. ***

## (3) THE QUEUE (w19_queue.py, each with its vote)

- WALL-CASCADE CENSUS BELOW KAPPA: kappa = 3 (the minimal closed-walk length with monodromy -1; the 144 unit-triangle walks, all -1). The census at m <= 6: 84/144/960/3360/16860 closed walks; ALL odd-length walks carry -1, all even-length +1; the monodromy = (-1)^{2A} (A = the normalized enclosed area), verified on 1020 walks (m = 3, 4) by shoelace. THE E-FLATNESS THEOREM [census + proof]: NO E-pure closed walk (alpha1/alpha2 steps only) has monodromy -1 — the E-connection is the discrete differential of the sector coordinate, so the exponent Σ<v_k, d_k> vanishes identically; every -1 requires rho-steps: THE CURVATURE IS V-CARRIED. VOTE: RE-OPEN-support (no E-side artifact below kappa).
- DRIFT-MONODROMY LIFT (consuming P-19): for each sector representative lambda in {alpha1, alpha2, rho}, the 9 two-step drift loops v -> v+lambda -> v+2lambda (mod 2Lambda): monodromy = +1 in BOTH connection gauges [machine-exact] — THE FLAT CERTIFICATE (the arithmetic <lambda, lambda> = 2 = 0 mod 2 and 2Q = 0): the level-2 lifts commute with the drift. This is the A2-analog of the W21 left-equivariance certificate (polar(t0.M) = t0.polar(M), 200/200). VOTE: RE-OPEN-support.
- LEVEL-L FIBRES (L = 2, 3): the sector-fibre graphs; at L=2: 4 sectors, the V-edges are exactly the 2 diagonals of the E-square; at L=3: 9 sectors, the V-edges are long chords; the E-subgraph alone is CONNECTED at both levels [machine] — the V-edges are redundant for connectivity, the fibres carry no 2-primary obstruction of their own (the torsion is Gamma-homology, the base side). VOTE: NEUTRAL-support.
- SEAM BATTERY: the 3 Weyl reflection axes (the coordinate swaps = the GKM walls): the Q-defects on all 19 corners = 0 (Q is coordinate-symmetric); the connection-equivariance c(rv, rd) = c(v, d) = 0 mismatches (the dot form is swap-invariant); the (s1 s2)^3 = id relation lifts with sign +1. The battery closes exactly, no compensator needed (the analog of the W20/W21 seam consistencies at 1008/1380 equations). VOTE: NEUTRAL-support.
- STAGE 3.5 (the CKM-chain trace, d(F) pinned): the maximal character-matched descent chains: triv and psi_rho books stall at w0; psi_a1: w0 -> s2s1 -> e (the composite rank-2 descent into e fires through the telescoped psi_{rho+alpha1} = psi_a2); psi_a2: w0 -> s1s2 -> e (mirror). d(F) pinned per book: 4*s2s1 / 4*s1s2. THE MOD-2 PAIRING: the firing coefficients are 4 = 0 mod 2, so the delta_1 induced diagonal VANISHES identically — d(F) is a mod-2 cycle trivially (the honest analog of the W19 C3-gate d_23.d(F) = 0). VOTE: SPLIT — the primary-obstruction route to delta_1 = 3/2 is dead (the pairing vanishes), but the torsion persists: the classes remain live candidates for the secondary obstruction.
- 13C STAGE 2a/2b: the 2-primary counts t_2(7..10) = [17, 19, 23, 25] — strictly increasing (the A2 book is UNTRUNCATED: the Gamma-homology grows linearly, qualitatively different from the n=4 campaign's finite [1,3,3,1] which wraps around the kappa-truncation); 2b: every 2-primary summand at every degree 1..10 is a plain Z/2 (125 summands, no Z/4, no Z/8) — matching the campaign's all-Z/2 finding at degree 9. VOTE: RE-OPEN-support.

## (4) THE ADJUDICATION

The aggregate: every item votes RE-OPEN or neutral; NONE votes to harden; the decisive (c)-scope proves the E/V-artifact mechanism vacuous on the analog object. THE VERDICT STANDS WITH WAVE 21: P-delta_1 re-opened; the primary-obstruction route dead in the analog book; the degree-9 classes persistent. The bracket [4/3, 3/2] for delta_1 intact; the secondary obstruction H^4(B_4; Z) remains the unpinned frontier.

TWO HONEST TENSIONS (the next-wave directives): (i) the analog count 23 exceeds the W18 PD-mirror bound of at most one genuine class — the reconciliation is that the analog book is unbounded (no kappa-truncation), so the decisive test is to transfer the firing-census and provenance machinery to the n=4 932-stratum complex itself, where the truncation lives; (ii) the psi_rho-book of (b) (the (Z/4)^2 orphan layer) exists as a coinvariant layer that does NOT feed the orbit complex — whether the n=4 book possesses an analogous silently-dropped layer is a falsifiable question to check before the re-open is treated as final. Then (iii) pin the secondary obstruction (the only live route to a value of delta_1) and (iv) the level-4 cross-check of the level-crossing certificate (this round, like W20/W21, certifies levels 2/3 only).

Deliverable: download/Wave19_cScope_Adjudication_Report.pdf (11 pages, all QA gates PASS). Scripts: scripts/w19_core.py, w19_cscope.py, w19_queue.py + outputs + JSON data. Qutrit verdict untouched: H_2(B_3) = Z/3, delta_2 = 4/3. Manuscripts untouched.
