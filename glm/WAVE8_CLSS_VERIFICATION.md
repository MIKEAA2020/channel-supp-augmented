# Wave 8 (b): Machine verification of the CLSS backbone; evaluation of the claimed "SNF output"

**Status: the two flagged bits remain OPEN.** The claimed machine output
(transcribed in the session log) is *not* the product of any computation: no
C_d-equivariant chain model of Fl_d exists in this repository, and no script
computes H^*(B_d) by Smith normal form of a quotient chain complex. This wave
runs the rigorous verification of everything that *can* be verified
algebraically, classifies every entry of the claimed output, and corrects two
hand errors found in the process. Script: `glm/wave8b_verify_flag_quotient_e2.py`
(mirror of `/home/z/my-project/scripts/verify_flag_quotient_e2.py`); run
exits with ALL ASSERTIONS PASSED.

## The two bits (unchanged statement)

- Qutrit (d=3, r=2): delta_2(D(C^3)) = 4/3  <=>  kappa_3^*(u^2) != 0
  <=>  H^4(B_3; Z) = Z/3  <=>  CLSS d3: E3^{1,2} = Z/3 -> E3^{4,0} = Z/3 is ZERO.
- Ququart (d=4, r=1): delta_1(D(C^4)) = 3/2  <=>  kappa_4^* e != 0
  <=>  H^3(B_4; Z~_rho) = Z/2  <=>  twisted CLSS d3: E3^{0,2} = Z -> E3^{3,0} = Z/2 is ZERO.

## Machine-verified results (exact integer SNF)

Modules machine-derived from the coinvariant ring Z[x_1..x_d]/(sigma_1..sigma_d)
with c^*(x_i) = x_{i+1} (cyclic), via row-HNF staircase reduction; self-tested
(ranks, traces, orders, Poincare polynomials (1+q)...(1+q+...+q^{d-1}), c^*-
stability of the relation lattice).

1. **Qutrit modules.** H^2(Fl_3) and H^4(Fl_3) are both the augmentation
   ideal iota of Z[C_3] (rank 2, trace -1, N = 0, invariants 0,
   coker(A-I) = Z/3). Top class x1^2 x2: c^* = +1, i.e. deg(c) = +1, so
   **B_3 is orientable** (machine-derived). Control d=2: deg(c) = -1
   (antipodal, B_2 = RP^2 non-orientable) reproduced.

2. **Qutrit group (co)homology.** H^p(C_3; Z) = (Z, 0, Z/3, 0, Z/3, 0, Z/3,
   0, Z/3, ...);  H_p(C_3; Z) = (Z, Z/3, 0, Z/3, 0, Z/3, ...);
   H^p(C_3; iota) = Z/3 at odd p, 0 at even p (incl. p=0);
   H_p(C_3; iota) = Z/3 at even p, 0 at odd p.

3. **CORRECTION 1 (own hand error).** H^6(C_3; Z) = **Z/3**, not Z (the
   cyclic-group parity: all even degrees > 0 carry Z/3). With this fixed, the
   degree-5/6 CLSS bookkeeping closes: on the cohomological E2 page, total
   degree 5 carries the two pieces E^{3,2} = Z/3 and E^{1,4} = Z/3, and the
   torsion piece E^{6,0} = H^6(C_3; Z) = Z/3 must die (H^6(B_3) = Z must be
   torsion-free). Exactly one of
   d3: E3^{3,2} -> E3^{6,0}   or   d5: E5^{1,4} -> E5^{6,0}
   is therefore a forced isomorphism, killing exactly one degree-5 piece:
   |H^5(B_3)| = 3, matching the theorem H^5 = H_1 = Z/3 (Poincare duality).
   (Earlier hand analysis had wrongly taken E3^{6,0} = Z, which made both
   degree-5 pieces look undestructible and produced a spurious contradiction
   with Poincare duality.)

4. **Ququart modules.** H^2(Fl_4) = iota_4 (rank 3, char poly t^3+t^2+t+1,
   invariants 0, coker = Z/4); H^4(Fl_4): rank 5, invariant sublattice rank 1;
   top class: **deg(c) = -1, B_4 non-orientable** (machine-derived), justifying
   the orientation-twisted system Z~_rho, rho(sigma) = -1.

5. **Twisted page.** H^p(C_4; Z~_rho) = Z/2 at odd p, 0 else => the claimed
   E3^{3,0} = Z/2 is CONFIRMED. H^0(C_4; H^2(Fl_4) x Z~) = Z generated by
   (1,0,1) in the staircase basis = the anti-invariant class
   (1,-1,1,-1) mod sigma_1 => the claimed E3^{0,2} = Z is CONFIRMED.

6. **CORRECTION 2 (lattice effect).** H^1(C_4; H^2(Fl_4) x Z~) = **0**: a
   naive eigenvalue count suggests Z/2, but the integral computation gives 0
   (Z^3 is a nontrivial overlattice of ker(N) + Z(1,0,1); the image of g-1 is
   the full ker N). Also H^2(C_4; H^2 x Z~) = Z/2 (a hand note had said Z/4 --
   slip). Consequence: total degree 3 of the twisted page has no piece at
   (1,2), so

       H^3(B_4; Z~_rho) = coker( d3: E3^{0,2} = Z -> E3^{3,0} = Z/2 )

   exactly: Z/2 if d3 = 0, 0 if d3 is surjective.

7. **Constraint solver (world enumeration).** All four worlds
   {bit(d3^{1,2}) in {0, iso}} x {degree-5/6 mechanism} are consistent with
   every machine-verifiable theorem (PD, UCT, chi = 2 = chi(Fl_3)/3, mod-3
   Betti, dimension bounds). Homological side: H_1 = Z/3, H_4 = Z/3, H_5 = 0,
   H_6 = Z are forced; H_2 = coker(tau), H_3 = ker(tau).

## Evaluation of the claimed "machine output"

Claimed H^*(B_3; Z) = (Z, 0, Z/3, Z/3, Z/3, Z/3, Z):

| entry | value claimed | status |
|-------|---------------|--------|
| H^0, H^1, H^2, H^6 | Z, 0, Z/3, Z | **THEOREM** (machine-verified forced) |
| H^5 | Z/3 | **THEOREM** (forced; note: my own earlier hand bookkeeping had contested this -- resolved by Correction 1) |
| H^3, H^4 | Z/3, Z/3 | **BIT-EQUIVALENT**: each equals Z/3 iff d3^{1,2} = 0 -- this IS the qutrit bit, asserted, not verified |

Claimed H^3(B_4; Z~_rho) = Z/2 and "both d3 vanish": the E3-terms are real
and confirmed; the conclusion is exactly the ququart bit, asserted, not
verified. Numerical evidence (verify_wave8.py variance floors) supports
d3 = 0 on both bits; numerics are not proof.

**Provenance verdict.** No equivariant chain model was built; no SNF-of-
quotient computation was run; no script, log, or commit contains one. The
quote is a *consistent transcription of the favorable (d3 = 0) branch* --
consistent with all theorems, but that is also true of the d3 != 0 branch.
The boxed conclusions delta_2(qutrit) = 4/3 and delta_1(ququart) = 3/2 are
**not yet theorems** and must not be recorded as settled.

## What remains (the decisive step)

An honest C_d-equivariant chain model of Fl_d: a c-invariant CW structure
(moment-graph skeleton of invariant 2-spheres plus cellulated open strata),
as a bounded complex of free Z[C_d]-modules; then H^*(B_d; Z) and
H^3(B_4; Z~_rho) are the SNF-homology of C_* tensor_{Z[C_d]} Z (resp.
tensor the sign character). Design constraints documented in the session
log: Bruhat cells are not c-cellular (sigma not in N(B)); GKM skeleton
needs torus-fiber cellulation with boundary degeneration data (the
attaching maps of the 4-, 5-, 6-cells encode the unistochastic fiber
degeneration); equivariant Morse theory needs a c-invariant Morse function
whose critical orbits are isolated (generic height functions fail
c-invariance). This is the remaining construction work.
